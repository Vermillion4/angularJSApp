import { SelectFirstLetterPipe } from './select-first-letter.pipe';

describe('SelectFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});

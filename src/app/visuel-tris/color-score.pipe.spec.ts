import { ColorScorePipe } from './color-score.pipe';

describe('ColorScorePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorScorePipe();
    expect(pipe).toBeTruthy();
  });
});
